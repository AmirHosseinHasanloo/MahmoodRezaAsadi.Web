@page "{id}"
@using Core.Convertors
@model MahmoodRezaAsadi.Web.Pages.Admin.Users.RecovryUserModel
@{
    ViewData["Title"] = "بازیابی کاربر بن شده";
}

<div class="clearfix">
    <br />
</div>

<div class="col-lg-8 col-md-8 col-xs-12 col-sm-12">
    <div class="panel panel-default">
        <div class="panel-heading">
            بازیابی کاربر
        </div>
        <div class="panel-body">
            <form method="post">
                <input type="hidden" name="userId" value="@Model.User.UserId" />
                <dl class="dl-horizontal">
                    <dt>نام کاربری</dt>
                    <dd>@Model.User.UserName</dd>
                    <br />
                    <dt>ایمیل</dt>
                    <dd>@Model.User.Email</dd>
                    <br />
                    <br />
                    <dt>تاریخ ثبت نام</dt>
                    <dd>@Model.User.CreateDate.ToShamsi()</dd>
                    <dt></dt>
                    <dd>
                        <input type="submit" class="btn btn-primary" value="بازیابی" />
                    </dd>
                    <br />
                </dl>
            </form>
        </div>
        <!-- /.panel-body -->
    </div>
    <!-- /.panel -->
</div>
